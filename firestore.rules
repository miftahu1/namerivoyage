rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access for the prototype phase.
    // WARNING: In a production app, you should restrict access to authenticated users.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}